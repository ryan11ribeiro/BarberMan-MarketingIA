# 💈 BarberMan - Automação de Marketing com IA

Esse é um script de automação que criei para resolver um problema real de barbearias: **a queda de movimento em dias de chuva ou calor extremo.**

Em vez de depender da sorte, esse robô monitora o clima e cria campanhas de marketing dinâmicas sozinho.

---

## 🤔 Qual é a ideia?

Ninguém gosta de sair de casa para cortar cabelo debaixo de chuva forte. O resultado? A barbearia fica vazia e o dono perde dinheiro.

Para resolver isso, desenvolvi um script em **Python** que:
1.  Consulta a previsão do tempo da cidade em tempo real.
2.  Identifica se a situação é crítica (Chuva, Calor excessivo ou Tempo bom).
3.  Usa **Inteligência Artificial (Llama 3 via Groq)** para escrever uma mensagem de WhatsApp curta e persuasiva, oferecendo um incentivo (ex: desconto ou brinde) baseado no clima.

---

## 🛠️ O que eu usei (Stack)

Fiz esse projeto para praticar integração de APIs e Engenharia de Prompt.

* **Python**: Para toda a lógica de automação e requisições.
* **Groq Cloud (Llama 3)**: Para gerar os textos de forma inteligente (sem usar frases prontas e repetitivas).
* ** Wttr.in**: Fonte de dados meteorológicos.
* **Dotenv**: Para gerenciar as chaves de API com segurança (não deixo senhas no código).

---

## ⚙️ Como funciona o fluxo

O código roda seguindo esta lógica:

1.  **Input:** O usuário define a cidade (ex: "João Pessoa").
2.  **Análise:** O script bate na API de clima.
3.  **Geração:** Ele envia esses dados para a IA com um prompt específico: *"Crie uma mensagem curta para o WhatsApp usando esse contexto..."*.
4.  **Output:** O script imprime a mensagem pronta para ser enviada.

---

## 🚀 Como rodar na sua máquina

Se quiser testar o gerador de copy:

1.  **Clone este repositório:**
    ```bash
    git clone [https://github.com/ryan11ribeiro/BarberMan-Marketing-IA.git](https://github.com/ryan11ribeiro/BarberMan-Marketing-IA.git)
    cd BarberMan-Marketing-IA
    ```

2.  **Instale as bibliotecas necessárias:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **Configure sua Chave:**
    Crie um arquivo `.env` na raiz da pasta e coloque sua chave gratuita da Groq (é rápido de pegar no site deles):
    ```env
    GROQ_API_KEY=gsk_sua_chave_aqui
    ```

4.  **Rode o script:**
    ```bash
    python marketing.py
    ```

---

## 📊 Exemplo de Resultado

Testei hoje para um dia de chuva e o retorno foi:

> **Cidade:** João Pessoa | **Clima:** Chuva leve
>
> **🤖 A IA sugeriu:**
> "🌧️ O tempo fechou aí fora, mas seu visual não precisa fechar também! A chuva tá caindo, e o preço também: venha cortar agora e ganhe **15% OFF** pra compensar o guarda-chuva. Corre pra cá! 💈"

---

Desenvolvido por **Ryan Ribeiro** 💻
